// Semver string. Bump rules (per spec):
//   patch: bug fix, score unchanged
//   minor: weight / threshold tweak, structure unchanged
//   major: structural change (add/remove a component)
export const SCORER_VERSION = "1.0.0";
